import styled from 'styled-components';

export const Charts = styled.div`
    z-index: 12;
    position: fixed;
    bottom: 30px;
    right: 30px;
    display: flex;
    gap: 0;
    border-radius: 20px;
    overflow: hidden;
`;