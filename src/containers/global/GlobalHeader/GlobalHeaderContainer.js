import GlobalHeader from '../../../components/global/GlobalHeader/GlobalHeader';

const GlobalHeaderContainer = () => {

    // 로그인 인증 여부 확인 > props 전달    

    return (
        <GlobalHeader />
    )
}

export default GlobalHeaderContainer;