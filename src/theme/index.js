import {
    fontSizes,
    fontWeights,
    colors,
    device
} from './base';

import {
    scrollWrapper,
    Fieldset,
    Input,
    SubmitButton,
} from './module';

export const theme = {
    fontSizes,
    fontWeights,
    colors,
    device
};

export const module = {
    scrollWrapper,
    Fieldset,
    Input,
    SubmitButton,
};